{
  "name": "masinloc-connect-mobile-app",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:mobile": "playwright test --project=mobile",
    "native:configure": "node scripts/configure-native-auth.mjs",
    "cap:sync": "npm run build && cap sync && npm run native:configure",
    "cap:ios": "npm run build && cap sync ios && npm run native:configure && cap open ios",
    "cap:android": "npm run build && cap sync android && npm run native:configure && cap open android"
  },
  "dependencies": {
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "^8.5.0",
    "@supabase/supabase-js": "^2.57.4",
    "lucide-react": "^1.37.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/ios": "^8.5.0",
    "@playwright/test": "^1.55.0",
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.2.2"
  }
}
